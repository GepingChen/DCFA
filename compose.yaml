services:
  dcfa-development-demo:
    build:
      context: .
    environment:
      DCFA_ACCESS_LOG: "0"
      DCFA_OUTPUT_ROOT: /app/artifacts/local/website-demo
      DCFA_SERVER_NAME: 0.0.0.0
      PORT: "7860"
    init: true
    ports:
      - "127.0.0.1:7860:7860"
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp:size=256m,mode=1777
    volumes:
      - dcfa-demo-artifacts:/app/artifacts

volumes:
  dcfa-demo-artifacts:
